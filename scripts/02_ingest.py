"""Step 2: chunk papers, optionally enrich with HQ, embed and upsert into Qdrant.

Two collections are populated, one per ingestion variant:
  - ezmed_chunks_plain        (baseline, qr_only)
  - ezmed_chunks_hq           (hq_only, both)"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
